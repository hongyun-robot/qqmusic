import 'package:flutter/material.dart';

class PurchasedPage extends StatelessWidget {
  const PurchasedPage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Text('PurchasedPage ');
  }
}
